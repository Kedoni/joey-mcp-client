• Bring your own API — Connect using any OpenAI-compatible API (base URL, optional bearer token, custom headers) as an alternative to OpenRouter
• Local device tools — Toggle on-device tools per chat for time, location, contacts, messages, calls, email, calendar, reminders, device info, URLs, and alarms
• Safer actions — SMS, calls, email, URLs, and alarms show clear confirmation cards before opening another app
• Bug fixes — Improved provider handling, local tool URI encoding, and iOS keyboard behavior while streaming